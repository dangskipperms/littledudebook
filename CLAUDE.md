Personalized illustrated book pipeline. Design docs: agents-plan.md.

Books live in a separate repo (path in `.books-path`). Active book name in `.active-book`.
Each book has its own `doc.md` at `{books-path}/{book-name}/doc.md`.

After every action, commit the changes with a short descriptive message.
